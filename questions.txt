1.管理模块
	① 获取记录（category）通过query.jsp，每次通过action从DB中取，
	     记录存入map中，以json格式传到前端
2.商品模块
	①s2sh文件上传时，若封装了文件的三个属性到一个类里，在文件上传时可取到FileName的值，
	file以及 contenttype为null。    不封装则没问题。file为临时文件后缀为temp，需设置临时
	文件存放目录，有默认值。
	②文件上传后，重新启动服务器，上传文件自动删除，解决方法1，将文件上传
	到工程目录之外，在server.xml中配置context属性，添加虚拟目录。
	③ 文件回显问题。文件重复上传问题。
	④ 刷新，重复提交问题。
	⑤库存问题